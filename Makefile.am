## Process this file with automake to generate Makefile.in.

if BUILD_EXAMPLES
EXAMPLEDIR = examples
else
EXAMPLEDIR =
endif

if BUILD_TOOLS
TOOLDIR = tools
else
TOOLDIR = 
endif

SUBDIRS = src $(TOOLDIR) $(EXAMPLEDIR)

